#pragma once
#include <windows.h>
#include <string>
#include <vector>


// extern "C" __declspec(dllexport) int KmpTextSearch(const char* targetText, const char* keyword, int* matchResult, int maxResultCount);
